import { Typography, Box } from '@mui/material';

const Dashboard = () => {
  return (
    <Box sx={{ padding: '50px 20px' }}>
      <Typography variant="h4">User  Dashboard</Typography>
      {/* Add user profile, emergency contacts, etc. */}
    </Box>
  );
};

export default Dashboard;
